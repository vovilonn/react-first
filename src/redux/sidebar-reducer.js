const initialState = {
    friends: [
        { name: "Vova", id: 1 },
        { name: "Mary", id: 1 },
        { name: "Antony", id: 1 },
    ],
};

const sidebarReducer = (state = initialState, action) => {
    return state;
};

export default sidebarReducer;
